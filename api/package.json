{
  "name": "api",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "create-migration": "bun run workspace/create-migration.ts",
    "run-migration": "wrangler d1 migrations apply boilerplate --local"
  },
  "dependencies": {
    "@hono/valibot-validator": "^0.5.2",
    "@pothos/core": "^4.3.0",
    "@prisma/client": "^6.3.1",
    "bcrypt-ts": "^5.0.3",
    "graphql-yoga": "^5.10.11"
  }
}
